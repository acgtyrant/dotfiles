nano /etc/locale.gen 
locale-gen 
ln -s /usr/share/zoneinfo/Asia/Shanghai /etc/localtime
hwclock --systohc --utc
echo ThinkPad.acgtyrant.com > /etc/hostname
passwd 
useradd -m -g users -G wheel,storage,power -s /bin/bash acgtyrant
pacman -S vim
pacman -S sudo
chmod u+w /etc/sudoers
vim /etc/sudoers
chmod u-w /etc/sudoers

open multilab repository

sudo pacman -S reflector
sudo reflector --verbose --country "China" -l 5 -p http --sort rate --save /etc/pacman.d/mirrorlist

sudo pacmatic -S git wqy-microhei python2 goagent dmenu i3 xorg-xrandr gvim feh gksu goagent gimp tmux openssh htop ranger alsi acpi scrot fcitx-configtool mplayer urxvt-perls gcc html2text linux-headers xf86-video-intel intel-dri clang cmake wgetpaste net-tools opera nodejs fcitx-rime kingsoft-office firefox cmatrix stow

yaourt 

add catalyst repo

yaourt -S chromium acroread mentohust-bin haroopad 

/etc/pacman.conf

git clone git@github.com/acgtyrant/bin.git ~/bin
git clone git@github.com/acgtyrant/dotfiles.git ~/dotfiles

/*~/.bashrc*/
/*~/.xinitrc*/
/*~/.config*/
/*~/.gitconfig*/
